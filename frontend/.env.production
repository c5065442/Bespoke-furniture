# Used automatically by `npm run build` (Vite's default "production" mode).
# Relative path: the built frontend is served by the same Django app as the
# API in production, so this resolves against whatever domain it's hosted
# on — no CORS configuration needed, and no domain hardcoded here.
VITE_API_BASE_URL=/api/v1
