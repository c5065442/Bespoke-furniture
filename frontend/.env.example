VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1

# No separate Stripe key needed here — the backend's
# create-payment-intent response includes the publishable key (set once,
# in backend/.env, as STRIPE_PUBLISHABLE_KEY).
